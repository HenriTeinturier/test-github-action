name: production workflow
on:
  push:
    branches: main

jobs:
  deploy-production:
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://mon-application.fr
    steps:
      - run: |
          echo Deploy to production
          # Pour récupérer le recret on utilise secret
          echo "secret password" ${{ secrets.DATABASE_PASSWORD }}
          # Pour récupérer une variable sur GITHUB on utilise vars
          echo "DATABASE_URL" ${{ vars.DATABASE_URL }}

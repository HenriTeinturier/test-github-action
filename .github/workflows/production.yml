name: production workflow
on:
  push:
    branches:
      - production

jobs:
  deploy-production:
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://mon-application.fr
    steps:
      - run: |
          echo Deploy to production
